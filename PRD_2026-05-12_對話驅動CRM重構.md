# MUSE CRM 重構產品需求文件（PRD）

> 從「分群行銷」到「對話為核心」的全面重構

| 項目 | 內容 |
|------|------|
| 文件版本 | v1.0 |
| 建立日期 | 2026-05-12 |
| 文件狀態 | Draft（待技術評審） |
| 產品定位 | 對話驅動 + AI 輔助 + 主管協作的客戶溝通平台 |
| 目標產業 | 石材建材（石英石、人造石、岩板） |
| 主要使用者 | 第一線客服、客服主管、業務窗口 |

---

## 1. 產品背景與重構動機

### 1.1 現況問題
目前的 MUSE CRM 以「**標籤分群 + 廣播觸達**」為核心商業邏輯，雖然具備多渠道接收（Messenger / Instagram / LINE）與 LLM 分析能力，但在實際營運中暴露下列問題：

1. **分群操作門檻高** — 第一線人員幾乎不維護標籤，導致分群質量不佳，廣播效果遞減。
2. **廣播 ROI 不明** — 建材業客單價高、決策週期長，群發訊息的轉換率遠低於 1-on-1 跟進。
3. **客服協作斷裂** — 客戶 24h window 內若客服請假/休假，沒有交班機制；棘手客戶也無法快速升級給主管。
4. **AI 能力孤立** — 既有 LLM 分析結果僅作為「事後標籤」，無法在客服「正要回覆的當下」提供幫助。

### 1.2 產品核心理念轉變

| 維度 | 舊系統 | 新系統 |
|------|--------|--------|
| 核心物件 | 客戶（Contact） + 標籤（Tag） | **對話（Conversation）** |
| 操作模式 | 行銷導向（分群、廣播） | **服務導向（1-on-1 對話、即時協作）** |
| AI 定位 | 事後分析 / 自動標籤 | **即時副駕駛（Copilot）** |
| 團隊協作 | 個別作戰（客戶 assigned_to） | **接力協作（分配 → 接管 → 升級）** |
| 成功指標 | 觸達人數、廣播開啟率 | **首次回覆時間、轉換率、客戶滿意度** |

### 1.3 為什麼建材業適合此模式
- 客單價高（單筆報價常 10 萬以上），值得投入人力 1-on-1 溝通。
- 決策週期長（從詢價到下單常 2-4 週），需要持續對話跟進。
- 客戶身份明確（設計師 / 屋主 / 建材行 / 工班），AI 易於識別並客製化回應風格。
- 規格複雜（材質、尺寸、紋路、加工），客戶常需要主管或師傅級人員介入。

---

## 2. 範圍宣告（Scope）

### 2.1 移除（Out of Scope / Deprecated）

| 模組 | 處理方式 | 理由 |
|------|---------|------|
| Tag 分群系統 | **完全移除** Tag / ContactTag / 分類概念 | 不再支援以分群為基礎的操作 |
| Broadcast 批量廣播 | **完全移除** Broadcast 模型 / 廣播頁面 / 排程 | 與「直面對話」理念不符 |
| Tag-based 客戶篩選 | 改為以「客戶屬性 + 對話狀態 + 自由文字搜尋」篩選 | — |
| 標籤分佈儀表板 | 移除 dashboard 中 tag-distribution | — |

### 2.2 保留並強化（Refactor & Enhance）

| 模組 | 變更重點 |
|------|---------|
| 多頻道接收（Webhook） | 維持 Meta + LINE Adapter，無架構變動 |
| 收件匣（Inbox） | **升級為新系統的核心頁面**，加入分配、接管、AI 副駕駛 UI |
| 客戶資料（Contact） | 簡化欄位，移除標籤關聯，新增「客戶身份」、「銷售階段」結構化欄位 |
| LLM 分析（Analysis） | 從「事後產出」改為「即時觸發 + 串流回應」 |
| 待辦動作（Action） | 維持，但與對話強耦合（每個 Action 必須有 conversation_id） |
| 報價（Quote）、產品（Product）、庫存 | 維持現狀，作為對話中的「附帶能力」 |
| 使用者 / 角色 / 權限 | 維持 RBAC 架構，新增「對話可見範圍」與「接管權限」 |

### 2.3 新增（New Features）

| 模組 | 說明 |
|------|------|
| **對話分配系統** | 主管或自動規則將對話分配給客服 |
| **主管接管機制** | 主管可旁聽、接管、轉接、強制介入任一對話 |
| **客服求援機制** | 客服可在對話中「舉手」請求主管協助 |
| **AI Copilot 側欄** | 即時生成回覆草稿、摘要、客戶意圖、知識庫檢索 |
| **對話狀態機** | 明確的對話生命週期（待認領 → 進行中 → 等待客戶 → 已解決 → 已關閉） |
| **知識庫（Knowledge Base）** | 產品 FAQ、規格表，供 AI RAG 檢索 |
| **協作備註（Internal Notes）** | 對話內私密訊息，僅團隊可見 |

---

## 3. 目標使用者與情境（Personas & Scenarios）

### 3.1 角色定義

#### 角色 A：客服（Agent / Role: `user`）
- **職責**：負責回覆客戶詢問，處理基本規格、報價、預約。
- **痛點**：遇到專業問題不確定怎麼回；同時開多個對話容易漏訊息。
- **關鍵需求**：AI 提供回覆草稿、可一鍵呼叫主管、清楚知道哪些對話是「我的」。

#### 角色 B：主管（Supervisor / Role: `manager`）
- **職責**：分配對話、培訓客服、處理升級案件、追蹤團隊績效。
- **痛點**：不知道客服當下對話進行得如何；客服離線時客戶訊息石沉大海。
- **關鍵需求**：所有對話的鳥瞰視角、一鍵接管、客服求援即時通知。

#### 角色 C：管理員（Admin / Role: `admin`）
- **職責**：使用者帳號、角色權限、AI 參數、系統設定。
- **關鍵需求**：權限管理、AI 成本監控、系統健康度。

### 3.2 核心情境（User Stories）

#### S1：對話流入與自動分配
> 一位設計師透過 Instagram DM 詢問「米白色岩板有沒有 90×270 的尺寸」。系統收到訊息後 AI 即時識別意圖為「規格詢問」，客戶身份為「設計師」，自動分配給負責岩板的客服小陳，並在小陳的收件匣彈出未讀提示。

#### S2：AI 副駕駛輔助回覆
> 小陳點開對話，右側 AI Copilot 自動顯示：（a）客戶意圖摘要、（b）相關產品連結（從知識庫檢索）、（c）建議回覆草稿三則。小陳選擇其中一則草稿，微調後送出。

#### S3：客服求援與主管接管
> 客戶接著問「能不能提供 SGS 抗污測試報告，順便讓師傅評估能否做 45 度導角？」。小陳不確定該款岩板是否有報告，點擊對話內的「求援」按鈕，附上備註「不確定報告與導角規格」。主管阿凱在通知區看到求援，點擊「接管對話」，系統將對話狀態改為「主管處理中」，由主管直接回覆客戶。

#### S4：主管被動旁聽
> 阿凱平時在收件匣首頁的「團隊視圖」可看到所有進行中的對話，AI 自動標記出三則「情緒偏負面」或「超過 30 分鐘未回」的對話，阿凱可選擇旁聽或主動接管。

#### S5：對話關閉與後續追蹤
> 客戶下單後，客服將對話標記為「已解決」。系統建立一筆 Action「7 天後回訪確認施工狀況」，到期時主動提醒。Action 完成時，對話狀態保持「已關閉」並可隨時重啟。

---

## 4. 功能需求（Functional Requirements）

### F1. 統一收件匣（Unified Inbox）★ 核心

#### F1.1 收件匣三種視圖
| 視圖 | 可見對象 | 內容 |
|------|---------|------|
| 我的對話 | 所有人 | 分配給當前使用者的對話 |
| 待認領 | 所有人 | 尚未分配的對話 |
| 團隊視圖 | manager / admin | 所有對話（含其他客服的） |

#### F1.2 對話列表欄位
- 客戶頭像 / 姓名 / 主要渠道圖示
- 最後一則訊息預覽 + 時間（相對時間，例：3 分鐘前）
- 未讀紅點
- AI 標記（🔴 負面情緒 / ⚠️ 超時未回 / 🆘 客服求援）
- 對話狀態徽章（待認領 / 進行中 / 等待客戶 / 主管處理中 / 已解決 / 已關閉）

#### F1.3 對話列表篩選
- 狀態篩選（多選）
- 渠道篩選（Messenger / IG / LINE）
- 負責人篩選（manager / admin 才可用）
- 客戶身份篩選（設計師 / 屋主 / 建材行 / 工班 / 未分類）
- 全文搜尋（客戶名、訊息內容）

> **禁止**：不再提供「按標籤篩選」、「按分群篩選」。

### F2. 對話狀態機與生命週期

```
                    [新訊息進入]
                         │
                         ▼
                  ┌─────────────┐
                  │  待認領      │
                  └─────────────┘
                         │ 自動分配規則 / 主管手動分配
                         ▼
              ┌──────────────────────┐
              │      進行中           │◀─┐
              └──────────────────────┘  │
                  │      │       │     │
        客戶回覆  │   客服回覆  │   求援/接管 │
                  ▼      ▼       ▼     │
              ┌──────┐ ┌──────┐ ┌──────────────┐
              │等待客戶│ │等待客戶│ │ 主管處理中    │──┘
              └──────┘ └──────┘ └──────────────┘
                  │             │
                  └─────┬───────┘
                        │ 「標記已解決」
                        ▼
                  ┌─────────────┐
                  │  已解決      │
                  └─────────────┘
                        │ 24h 無新訊息 / 手動關閉
                        ▼
                  ┌─────────────┐
                  │  已關閉      │
                  └─────────────┘
                        │ 客戶再次來訊
                        ▼
                  （重啟為「進行中」）
```

#### F2.1 對話狀態定義
| 狀態 | 觸發條件 | 顯示位置 |
|------|---------|---------|
| `unassigned` | 新對話、無客服負責 | 待認領視圖 |
| `active` | 已分配且最近一則訊息為客服發出 | 我的對話、團隊視圖 |
| `waiting_customer` | 客服已回覆，等待客戶 | 我的對話、團隊視圖 |
| `escalated` | 客服求援，主管尚未接管 | 主管通知區、團隊視圖 |
| `supervisor_taken` | 主管已接管 | 主管的我的對話 |
| `resolved` | 手動標記已解決 | 已解決視圖（可篩選） |
| `closed` | 24h 無新訊息或手動關閉 | 已關閉視圖（可篩選） |

### F3. 對話分配與接管機制 ★ 核心

#### F3.1 自動分配規則
**規則優先級（由高至低）：**
1. **沿用上次負責人** — 若該客戶過去 30 天內被分配過，回到同一客服。
2. **AI 路由** — 根據客戶意圖（pricing / spec / complaint 等）匹配「擅長標籤」最多的線上客服。
3. **輪詢分配** — 平均分配給線上的客服。
4. **降級為待認領** — 若無客服線上，狀態維持 `unassigned`。

#### F3.2 手動分配
- 主管在「待認領」、「團隊視圖」可手動指派 / 重新指派。
- 客服可將自己的對話「轉交」給其他客服（需被轉交方接受，或主管直接拍板）。

#### F3.3 主管接管
| 動作 | 觸發者 | 效果 |
|------|--------|------|
| 旁聽（Watch） | manager / admin | 對話加入「監聽中」，主管可看到所有訊息與客服回覆，但不發言 |
| 接管（Take Over） | manager / admin | 狀態轉為 `supervisor_taken`，主管成為實際回覆者；原客服改為「協作旁聽」 |
| 強制接管（Force Take） | admin only | 不需任何條件即可接管，記錄至 audit log |
| 歸還（Return） | 接管中的主管 | 將對話交還原客服，狀態回 `active` |
| 升級（Escalate） | 客服 | 主動標記「需要主管」，狀態轉 `escalated`，通知所有 manager |

#### F3.4 客服求援（Help Request）
- 客服在對話中點擊「🆘 求援」按鈕。
- 必須填寫「求援原因」（短文字 ≤ 200 字）。
- 系統發送主管端推播 + WebSocket 通知。
- 主管收到通知後可選擇「接管」或「指導」（在 Internal Note 內留下指引給客服）。

### F4. AI 副駕駛（AI Copilot）★ 核心

#### F4.1 即時意圖識別
- 每則新訊息進入時，由輕量 LLM（Gemini Flash Lite 或 Claude Haiku）即時分類：
  - **意圖**：pricing / spec / visit / complaint / followup / order / other
  - **客戶身份**：designer / homeowner / dealer / contractor / unknown
  - **情緒**：positive / neutral / negative
  - **緊急度**：high / medium / low

#### F4.2 對話摘要（Conversation Summary）
- 每 5 則訊息或對話被開啟時，自動更新摘要。
- 摘要內容：客戶背景、目前需求、已討論事項、待解決問題。
- **目的**：主管接管時可 30 秒內掌握脈絡。

#### F4.3 智能回覆草稿（Reply Suggestions）★ 重點
- 客服輸入框上方顯示 3 則 AI 建議回覆。
- 點擊任一則 → 文字進入輸入框可編輯後送出。
- 採用 RAG：結合產品知識庫、過往該客戶對話、品牌語氣設定。
- 模型：`anthropic/claude-sonnet-4-6`（高品質）+ `gemini-flash-lite`（兜底）。

#### F4.4 知識庫檢索（Knowledge Retrieval）
- 偵測客戶提到的產品 / 規格關鍵字，自動從知識庫拉出相關卡片。
- 卡片內容：產品圖、規格表、價格區間、常見 Q&A。
- 客服可一鍵將卡片「拼貼」到回覆訊息（自動產出條列文字）。

#### F4.5 風險預警
- AI 偵測「客戶將要流失」、「投訴可能升級」、「金額異動高風險」時，在對話頂端顯示警示條。
- 觸發條件範例：
  - 連續 2 則訊息情緒 = negative
  - 客戶提到「退貨 / 退費 / 客訴 / 律師」等關鍵字
  - 客戶詢問報價後超過 7 天未回應

#### F4.6 AI 設定與監督
- Admin 可在設定頁配置：
  - 主模型 / 兜底模型
  - 是否啟用「即時草稿」
  - 預算上限（日 / 月）
  - 品牌語氣（formal / friendly / professional）
  - 知識庫資料來源

### F5. 客戶資料管理（精簡版）

#### F5.1 簡化的 Contact 欄位
**保留：**
- `display_name`, `avatar_url`, `phone`, `email`
- `customer_identity`（設計師 / 屋主 / 建材行 / 工班 / 未分類）—— 取代過去的 Tag 身份
- `sales_stage`（初次接觸 / 評估中 / 已報價 / 已成交 / 已流失）—— 新增結構化欄位
- `assigned_to`（負責客服）
- `notes`（自由文字備註，主管 / 客服共用）

**移除：**
- `Tag` / `ContactTag` 整套移除
- `Contact.intent` / `budget_range` / `preferred_products`（改由 AI 從對話即時推斷，不持久化於 Contact）

#### F5.2 客戶詳情頁
- 上半部：基本資料、客戶身份、銷售階段、負責客服。
- 下半部三欄頁籤：
  - **對話歷史**：所有跨渠道對話列表，可點擊進入。
  - **報價與訂單**：與該客戶相關的所有 Quote。
  - **內部備註**：協作者留下的私密筆記（依時間倒序）。

> **移除**：客戶身上不再有「標籤」這一欄。

### F6. 內部協作備註（Internal Notes）

#### F6.1 在對話內留 Note
- 對話訊息流中可插入「內部備註」訊息類型，僅團隊可見、不會發送給客戶。
- 視覺上以黃底框區分。
- 支援 @提及（@小陳 @主管），被提及者收到通知。

#### F6.2 在客戶詳情頁留 Note
- 客戶詳情頁的 `notes` 區塊（沿用 UserNote 模型）。

### F7. 待辦動作（Actions）

#### F7.1 與對話強綁定
- 所有 Action 必須關聯一個 `conversation_id`（不再允許脫離對話的純客戶 Action）。
- Action 可由 AI 從對話建議（沿用既有機制），或客服 / 主管手動建立。

#### F7.2 視圖整合
- Action 列表新增「來源對話」連結，一鍵跳轉。
- 對話側邊欄顯示「此對話的 Action」清單。

### F8. 通知系統

#### F8.1 通知類型
| 事件 | 通知對象 | 通道 |
|------|---------|------|
| 新訊息到達（未分配） | 線上所有 manager | WebSocket + UI 紅點 |
| 對話分配給我 | 該客服 | WebSocket + UI 紅點 + 桌面通知 |
| 客服求援 | 所有 manager | WebSocket + UI 紅點 + 桌面通知 + 音效 |
| 我的對話被主管接管 | 原客服 | WebSocket + UI 提示 |
| @提及我 | 被提及者 | WebSocket + UI 紅點 |
| Action 即將到期 | Action 負責人 | WebSocket + 每日早晨 Email |
| AI 風險預警觸發 | 對話負責人 + manager | WebSocket + 對話頂端警示條 |

#### F8.2 通知偏好
- 沿用既有 `NotificationPreference` 模型，新增 supervisor / help_request 兩類偏好。

### F9. 報價、產品、庫存（維持現狀）

- 沿用既有 Quote / Product / StockLog / 外部庫存 proxy。
- 對話內可一鍵插入產品卡片（搭配 F4.4 知識庫）。
- 對話內可一鍵建立報價單（預填客戶資料）。

### F10. 儀表板（重新設計）

#### F10.1 主管儀表板
- 對話流入量（按渠道、按時段）
- **首次回覆時間 P50 / P90**（核心 KPI）
- **解決率**（已解決對話 / 進入對話總數）
- 求援率 / 接管率
- 客服在線狀態與工作量
- AI 副駕駛使用率（草稿採用率 / 編輯率）

#### F10.2 移除的儀表板項目
- 標籤分佈圖表
- 廣播相關統計（發送量、開信率等）

---

## 5. 資料模型變更計畫

### 5.1 資料庫變更總表

| 表 | 變更類型 | 說明 |
|----|---------|------|
| `tags` | **刪除** | 全表移除 |
| `contact_tags` | **刪除** | 全表移除 |
| `broadcasts` | **刪除** | 全表移除 |
| `contacts` | 修改 | 移除 `intent`, `budget_range`, `preferred_products`；新增 `customer_identity`, `sales_stage` |
| `conversations` | 修改 | 擴充 `status` enum；新增 `current_handler_id`, `supervisor_id`, `watchers[]`, `escalated_at`, `escalation_reason` |
| `messages` | 修改 | 新增 `is_internal`（內部備註）、`mentions[]` |
| `analyses` | 修改 | 沿用，新增 `risk_flags[]`（流失 / 投訴 / 高金額預警） |
| `actions` | 修改 | `conversation_id` 改為 NOT NULL |
| `users` | 維持 | 沿用 role / team_id |
| `roles` | 維持 | 新增系統角色：`agent`, `supervisor`, `admin`（取代 user/manager/admin 三級命名） |
| `knowledge_base` | **新增** | 知識庫條目（標題、內容、向量、來源） |
| `conversation_events` | **新增** | 對話狀態變更 audit log（誰、什麼時候、做了什麼） |
| `reply_suggestions` | **新增** | AI 生成的回覆草稿（用於後續分析採用率） |

### 5.2 新表結構示意

#### `knowledge_base`
```sql
CREATE TABLE knowledge_base (
  id UUID PRIMARY KEY,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  category VARCHAR(50),  -- product / faq / policy / spec
  source_url TEXT,
  embedding VECTOR(1536),  -- 需開啟 pgvector
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
CREATE INDEX ON knowledge_base USING ivfflat (embedding vector_cosine_ops);
```

#### `conversation_events`
```sql
CREATE TABLE conversation_events (
  id UUID PRIMARY KEY,
  conversation_id UUID REFERENCES conversations(id),
  event_type VARCHAR(50),  -- assigned / taken_over / escalated / returned / resolved / closed / reopened
  actor_id UUID REFERENCES users(id),
  target_id UUID REFERENCES users(id),  -- 被指派者
  metadata JSONB,  -- 求援原因、接管備註等
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### `reply_suggestions`
```sql
CREATE TABLE reply_suggestions (
  id UUID PRIMARY KEY,
  message_id UUID REFERENCES messages(id),
  suggestions JSONB,  -- [{"text": "...", "confidence": 0.8, "kb_refs": [...]}]
  used_suggestion_index INT,  -- 客服採用了第幾則
  edited_before_send BOOLEAN,  -- 是否編輯過
  generated_at TIMESTAMPTZ,
  model VARCHAR(100)
);
```

### 5.3 資料遷移策略
1. **Phase 0（部署前）**：備份完整資料庫至 Railway S3。
2. **Phase 1（資料保留）**：將 Tag 對應的客戶身份資訊（如 `designer`, `homeowner`）轉寫入新欄位 `contacts.customer_identity`。
3. **Phase 2（資料下線）**：將 `tags`, `contact_tags`, `broadcasts` 整表 export 至備份檔，再 DROP。
4. **Phase 3（重新分析）**：觸發既有對話的 Analysis 重跑，補齊 `customer_identity`、`sales_stage`、`risk_flags`。

---

## 6. API 變更計畫

### 6.1 移除的端點
```
DELETE  /api/v1/tags/*
DELETE  /api/v1/broadcasts/*
DELETE  /api/v1/dashboard/tag-distribution
DELETE  /api/v1/contacts/{id}/tags
```

### 6.2 修改的端點
| 端點 | 變更 |
|------|------|
| `GET /api/v1/contacts` | 移除 `tag_ids` 篩選參數，新增 `customer_identity`, `sales_stage` 篩選 |
| `GET /api/v1/inbox/conversations` | 新增 `view` 參數（`mine` / `unassigned` / `team`），新增 `handler_id`, `escalated` 篩選 |
| `POST /api/v1/inbox/conversations/{id}/messages` | 新增 `is_internal` 欄位、`mentions[]` 欄位 |

### 6.3 新增的端點

#### 對話分配與接管
```
POST   /api/v1/inbox/conversations/{id}/assign        # 分配給某客服
POST   /api/v1/inbox/conversations/{id}/escalate      # 客服求援
POST   /api/v1/inbox/conversations/{id}/take-over     # 主管接管
POST   /api/v1/inbox/conversations/{id}/return        # 主管歸還
POST   /api/v1/inbox/conversations/{id}/watch         # 主管旁聽
DELETE /api/v1/inbox/conversations/{id}/watch         # 取消旁聽
POST   /api/v1/inbox/conversations/{id}/resolve       # 標記已解決
POST   /api/v1/inbox/conversations/{id}/reopen        # 重啟對話
GET    /api/v1/inbox/conversations/{id}/events        # 取得對話 audit log
```

#### AI Copilot
```
GET    /api/v1/ai/suggestions?conversation_id=...     # 取得回覆草稿（串流）
POST   /api/v1/ai/suggestions/{id}/used               # 紀錄草稿被採用
GET    /api/v1/ai/summary?conversation_id=...         # 對話摘要
GET    /api/v1/ai/knowledge-search?q=...              # 知識庫檢索
```

#### 知識庫
```
GET    /api/v1/knowledge-base
POST   /api/v1/knowledge-base
PATCH  /api/v1/knowledge-base/{id}
DELETE /api/v1/knowledge-base/{id}
POST   /api/v1/knowledge-base/import                  # 批量匯入 CSV / Notion
```

### 6.4 WebSocket 事件變更

| 事件名稱 | 用途 |
|---------|------|
| `conversation.assigned` | 對話分配 |
| `conversation.escalated` | 客服求援 |
| `conversation.taken_over` | 主管接管 |
| `conversation.returned` | 主管歸還 |
| `conversation.resolved` | 對話已解決 |
| `ai.suggestion.ready` | AI 草稿生成完成（串流結束） |
| `ai.risk.detected` | 風險預警觸發 |
| `notification.mention` | 內部備註被 @ 提及 |

---

## 7. 頁面 / UX 變更計畫

### 7.1 移除的頁面
- `/broadcast/*` 整個目錄
- `/contacts/import-dialog.tsx` 中的「標籤匯入」邏輯（保留客戶基本欄位匯入）
- `/settings` 中的「標籤管理」分頁

### 7.2 重新設計的頁面

#### 新版 `/inbox`（三欄式）
```
┌────────────┬──────────────────────┬────────────────────┐
│            │                      │                    │
│  視圖切換   │   對話訊息流          │   AI Copilot       │
│  ────       │   ─────              │   ─────            │
│  我的對話   │   [客戶訊息泡泡]     │   📊 客戶意圖摘要   │
│  待認領     │   [客服訊息泡泡]     │   👤 客戶背景       │
│  團隊視圖   │   [內部備註(黃)]     │   📚 知識庫卡片     │
│  已解決     │                      │   ✍️ 回覆草稿 ×3    │
│  已關閉     │   ──────────────     │   ⚠️ 風險預警       │
│            │   [輸入框]           │                    │
│  對話列表   │   [傳送 / 草稿 / 求援]│                    │
│  ────       │                      │                    │
│  陳○○ 🔴   │                      │                    │
│  王○○ 🆘   │                      │                    │
│  李○○      │                      │                    │
│            │                      │                    │
└────────────┴──────────────────────┴────────────────────┘
```

#### 新版 `/dashboard`
- 第一屏：對話流入、回覆時間、解決率（主管最關心）
- 第二屏：客服在線狀態、工作量分配、AI 副駕駛使用率
- 第三屏：產品 / 銷售階段轉換漏斗

#### 新版 `/settings`
- 個人偏好（通知、外觀）
- 團隊管理（使用者、角色、權限）
- AI 設定（模型、預算、語氣）
- 知識庫管理（新增分頁）
- 自動分配規則設定

### 7.3 UI 元件變更
- 移除：`tag-pill`, `tag-selector`, `broadcast-form`
- 新增：`copilot-panel`, `escalation-button`, `take-over-banner`, `internal-note-bubble`, `kb-card`

---

## 8. AI 能力詳細設計

### 8.1 模型策略

| 任務 | 模型 | 模式 | 預期延遲 |
|------|------|------|---------|
| 即時意圖分類 | `gemini-flash-lite` | 同步呼叫 | < 500ms |
| 對話摘要 | `claude-haiku` | 異步（Celery） | < 3s |
| 回覆草稿生成 | `claude-sonnet-4-6` | 串流（SSE） | TTFB < 1s |
| 知識庫向量檢索 | `text-embedding-3-small` | 同步 | < 200ms |
| 風險預警判斷 | `gemini-flash-lite` | 異步 | < 2s |

### 8.2 RAG 知識庫設計
```
[客戶訊息]
    ↓
[Query 改寫 — 用 LLM 把口語轉換為檢索 query]
    ↓
[向量檢索（pgvector）+ 全文檢索（PG full-text）]
    ↓
[Top-K=5 知識條目]
    ↓
[Re-rank by 相關性 + 時效性]
    ↓
[組裝 prompt：客戶意圖 + 對話歷史 + 知識條目 + 品牌語氣]
    ↓
[Claude Sonnet 生成 3 則回覆草稿]
    ↓
[串流回傳前端]
```

### 8.3 成本控制
- 沿用既有 `LlmUsageLog` 紀錄每次呼叫。
- 新增**對話成本上限**：單一對話 LLM 呼叫超過 N 元時，降級為僅輕量模型。
- 新增**每日預算門檻**：80% 警示、95% 自動關閉草稿功能（保留摘要與意圖分類）。

### 8.4 隱私與資料外流
- 所有 LLM 呼叫透過 OpenRouter，需確認 zero-retention 設定。
- 客戶手機號 / Email 在送至 LLM 前進行 PII 遮罩（保留語義佔位符）。
- 敏感類別（投訴、退款）對話的回覆草稿需主管簽核才能送出。

---

## 9. 權限模型

### 9.1 角色與權限矩陣

| 動作 | agent | supervisor | admin |
|------|-------|-----------|-------|
| 看自己的對話 | ✅ | ✅ | ✅ |
| 看所有對話 | ❌ | ✅ | ✅ |
| 認領待認領對話 | ✅ | ✅ | ✅ |
| 分配對話給他人 | ❌ | ✅ | ✅ |
| 旁聽他人對話 | ❌ | ✅ | ✅ |
| 接管他人對話 | ❌ | ✅ | ✅ |
| 強制接管（無視 lock） | ❌ | ❌ | ✅ |
| 求援（escalate） | ✅ | ❌ | ❌ |
| 標記已解決 | ✅ 自己的 | ✅ 全部 | ✅ |
| 看儀表板 | ❌（僅看自己 KPI） | ✅ | ✅ |
| 管理使用者 | ❌ | ❌ | ✅ |
| 管理知識庫 | ❌（唯讀） | ✅ | ✅ |
| 修改 AI 設定 | ❌ | ❌ | ✅ |

### 9.2 對話資料可見範圍
- agent 只看 `current_handler_id = self.id` 或 `unassigned` 的對話。
- supervisor 看所有 `team_id = self.team_id` 的對話。
- admin 看全部。

---

## 10. 非功能性需求（NFR）

### 10.1 效能
- 對話列表載入：P95 < 500ms（含分頁）
- 訊息發送至客戶端：P95 < 1s（WebSocket 推送）
- AI 草稿首字回應（TTFB）：P95 < 1.5s
- 知識庫檢索：P95 < 300ms

### 10.2 可用性
- 服務 SLA：99.5%（單月停機 < 3.6 小時）
- Webhook 接收必須冪等（沿用既有 `meta_message_id` 去重）

### 10.3 安全
- JWT 沿用既有 24h access + 30d refresh
- 所有對話操作必須通過 RBAC 中介層
- 所有狀態變更寫入 `conversation_events` audit log
- 客戶 PII 在 LLM 呼叫前必須遮罩

### 10.4 可觀測性
- Sentry 接入後端 + 前端
- 對話狀態變更 metric（依小時聚合）
- AI 呼叫成功率 / 延遲 / 成本 metric
- WebSocket 連線數 / 推送失敗率 metric

### 10.5 成本
- LLM 月預算上限：USD 200（初期）
- 預算超 80% 時 Slack 告警，超 95% 自動降級

---

## 11. 里程碑與分期（Roadmap）

### Phase 1（4 週）— 移除 + 收件匣重構
**目標**：分群系統下線，新版收件匣可運作（不含 AI Copilot）

- 資料庫 schema 變更（Tag / Broadcast 下線）
- API 端點移除 / 修改
- 新版 `/inbox` 三欄式 UI
- 對話狀態機（`unassigned` / `active` / `waiting_customer` / `resolved` / `closed`）
- 手動分配機制

**驗收**：客服可在新版收件匣完成 1-on-1 對話的全流程。

### Phase 2（4 週）— 主管接管 + 協作
**目標**：主管接管機制完整，內部備註上線

- 主管旁聽 / 接管 / 歸還 / 強制接管
- 客服求援按鈕與通知
- 內部備註訊息類型 + @提及
- 對話 audit log
- 自動分配規則 v1（沿用上次負責人 + 輪詢）

**驗收**：模擬 10 次「客戶找上門 → 客服求援 → 主管接管 → 結案」流程零失敗。

### Phase 3（5 週）— AI Copilot 上線
**目標**：AI 在客服回覆當下提供幫助

- 即時意圖分類（沿用 Phase 1 的 quick_triage）
- 對話摘要（5 則訊息或開啟時觸發）
- 知識庫 schema + 上傳工具 + pgvector 部署
- 回覆草稿生成（串流 SSE）
- 風險預警（負面情緒、流失預警）

**驗收**：草稿採用率 ≥ 30%、對話平均回覆時間下降 30%。

### Phase 4（3 週）— 儀表板、優化、Hardening
**目標**：可上線到正式營運

- 新版主管儀表板
- AI 路由（依意圖分配對應擅長客服）
- 通知偏好擴充
- 成本與效能優化
- 完整 e2e 測試

**驗收**：可邀請真實客戶試用 2 週，主管 NPS ≥ 50。

### 整體時程：16 週（約 4 個月）

---

## 12. 成功指標（Success Metrics）

### 12.1 北極星指標
**對話 30 天轉換率**（從對話建立到產生報價單或訂單的比率）  
目標：從目前 X% 提升至 X+15%

### 12.2 輔助指標
| 指標 | 目前 | 目標 |
|------|------|------|
| P50 首次回覆時間 | 待測量 | < 5 分鐘 |
| P90 首次回覆時間 | 待測量 | < 30 分鐘 |
| 對話解決率（resolved / total） | 待測量 | ≥ 75% |
| 客服求援平均回應時間 | — | < 3 分鐘 |
| AI 草稿採用率 | — | ≥ 30% |
| AI 草稿編輯率（採用後修改） | — | ≤ 50% |
| 主管接管比例 | — | 5% ~ 15%（過低代表客服未受訓，過高代表客服素質不足） |

### 12.3 反指標（避免劣化）
- 客戶體驗變差：負面情緒對話數量上升
- 客服工作量爆炸：每客服平均同時處理對話數 > 8
- AI 成本失控：單月 LLM 支出 > USD 300

---

## 13. 風險與假設

### 13.1 主要風險

| 風險 | 影響 | 機率 | 緩解 |
|------|------|------|------|
| 移除分群 / 廣播後既有客戶不買單 | 高 | 中 | 提供「過去廣播紀錄」唯讀備份；提供「對話模板批量發送」過渡功能 |
| AI 草稿品質不佳，客服不採用 | 高 | 中 | Phase 3 內建 A/B 測試；提供 prompt 與 RAG 調優工具 |
| 主管接管引發客服反彈（信任問題） | 中 | 中 | 接管時對客服可見、透明；客服可在績效指標看到自己被接管率與原因 |
| pgvector 部署與 Railway 兼容問題 | 中 | 低 | Phase 3 前先做 spike，必要時改用外部向量資料庫（Pinecone / Qdrant） |
| LLM 成本超預算 | 中 | 中 | 多層降級策略 + 對話成本上限 |
| 資料遷移期間訊息漏接 | 高 | 低 | 凍結窗口 < 5 分鐘；遷移腳本可重跑（idempotent） |

### 13.2 關鍵假設
- 客戶端對「真人即時對話」的期待高於「廣播促銷」（與建材業客單價高、決策週期長一致）
- 客服團隊願意接受「主管旁聽 / 接管」（需要主管事先做好溝通）
- AI 副駕駛在繁體中文 + 建材業語境下表現可接受（需 Phase 3 驗證）
- Railway PostgreSQL 支援 pgvector 擴充

---

## 14. 未來規劃（Out of Scope for v1）

以下功能不在本次重構範圍，留待 v2+ 評估：
- 客戶端自助工具（線上選材、預約系統）
- 多語系支援（英文、簡中）
- 語音 / 影像通話整合
- 銷售漏斗自動推進（依客戶階段觸發 action）
- 客服績效 360 度評估
- AI 自動回覆（無客服介入的 fully-auto 模式）
- 跨團隊 / 跨分店的對話路由

---

## 15. 附錄

### 15.1 名詞表
| 術語 | 定義 |
|------|------|
| 對話（Conversation） | 一段時間內客戶與品牌的訊息往來，單一渠道 |
| 客戶（Contact） | 跨渠道整合後的單一客戶身份 |
| 客服（Agent） | 第一線回覆客戶的成員 |
| 主管（Supervisor） | 負責客服培訓、分配、接管的成員 |
| 接管（Take Over） | 主管暫時成為對話的實際回覆者 |
| 旁聽（Watch） | 主管被動觀察對話，不發言 |
| 求援（Escalate） | 客服主動請求主管協助 |
| 副駕駛（Copilot） | AI 在客服身旁提供建議的角色定位 |
| 知識庫（KB） | 產品、規格、FAQ 的結構化資料來源 |

### 15.2 相關文件
- 現有 CLAUDE.md（系統現況）
- 既有資料庫 schema（database/schema.sql）
- 既有 API 文件（待補）

### 15.3 變更記錄
| 版本 | 日期 | 變更內容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-05-12 | 初版 Draft | Claude（AI 產品經理） |

---

> **下一步行動**  
> 1. 與工程團隊評審本 PRD，確認技術可行性與工時估算。  
> 2. 與客服 / 主管團隊訪談，驗證使用情境與工作流程的真實性。  
> 3. 凍結 v1.0 範圍，建立 Phase 1 開發 Backlog。  
> 4. 規劃 pgvector spike，確認 Railway 部署可行性。
